describe('Centralized Data Vault service - POST', () => {
  describe('POST /:key { content }', () => {
    test('should respond 401 if not logged in', () => {

    })

    test('should respond 201 if logged in and key does not exist', () => {

    })

    test('should respond 400 with proper error if adding empty content', () => {

    })

    test('should add content associated to the given key (no replacing) if it exists in advance', () => {

    })

    test('should respond 400 with proper error if adding empty content', () => {

    })
  })
})
