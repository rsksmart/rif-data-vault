describe('Centralized Data Vault service - GET', () => {
  describe('/:key', () => {
    test('should respond 200 without being authenticated', () => {
      const key = ''
    })

    test('should retrieve an array of contents if the key exists', () => {

    })

    test('should retrieve an empty array if the key does not exist', () => {

    })
  })

  describe('/:key/:id', () => {
    test('should respond 200 without being authenticated', () => {

    })

    test('should retrieve a single content if key and id exists', () => {

    })

    test('should respond 404 if key does not exist', () => {

    })

    test('should respond 404 if id does not exist', () => {

    })
  })
})
