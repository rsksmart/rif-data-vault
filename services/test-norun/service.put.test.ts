describe('Centralized Data Vault service - PUT', () => {
  describe('PUT /:key/:id { content }', () => {
    test('should respond 401 if not logged in', () => {

    })

    test('should respond with a new id if both key and id provided', () => {

    })

    test('should respond with the proper cid of the given file', () => {

    })

    test('should replace all the key associated content if id is not provided', () => {

    })

    test('QUE HACE SI MANDA UN EMPTY CONTENT? BORRA? SETEA UN CONTENIDO VACIO? TIRA ERROR?', () => {

    })

    test('should respond 404 with proper error if key does not exist', () => { // 404 or create it?

    })

    test('should respond 404 with proper error if id does not exist', () => {

    })
  })
})
