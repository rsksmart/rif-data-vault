describe('Centralized Data Vault service - DELETE', () => {
  describe('DELETE /:key/:id { content }', () => {
    test('should respond 401 if not logged in', () => {

    })

    test('should respond 200 if not logged in', () => {

    })

    test('should delete all the content associated to the given key if id not provided', () => {

    })

    test('should delete just the content associated to the given key and id', () => {

    })

    test('should respond 404 with proper error if key does not exist', () => {

    })

    test('should respond 404 with proper error if id does not exist', () => {

    })
  })
})
